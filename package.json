{
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "start:backend": "cd Backend && npm start",
    "start:frontend": "cd vite-project && npm start",
    "start:socket":"cd Server && npm start",
    "start": "npm-run-all --parallel start:backend start:socket start:frontend"
  },
  "dependencies": {
    "cors": "^2.8.5"
  }
}
